from django.db import models
from django.contrib.auth.models import User

# Models

# class Booking(models.Model):
#     practitioner = models.ForeignKey(User, on_delete=models.DO_NOTHING, related_name="blog_posts")